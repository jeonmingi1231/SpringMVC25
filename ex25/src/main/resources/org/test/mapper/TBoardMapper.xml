<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.test.mapper.TBoardMapper">

	<select id="getList" resultType="org.test.domain.TBoardVO">
<![CDATA[
select * from test_board where bno > 0
]]>
	</select>

<insert id="insert">
insert into test_board (bno, title, content, writer)
values (seq_board.nextval, #{title}, #{content}, #{writer})
</insert>

<insert id="insertSelectKey">

<selectKey keyProperty="bno" order="BEFORE" resultType="long">
select seq_board.nextval from dual
</selectKey>

insert into test_board (bno,title,content,writer)
values (#{bno}, #{title}, #{content}, #{writer})
</insert>

<select id="getList2" resultType="org.test.domain.TBoardVO">
	SELECT * FROM test_board
</select>

</mapper>